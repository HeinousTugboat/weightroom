
extends templates/base.pug

block content
    div.wrestler-list
        p Workouts
        table
            tr
                th View
                th Name
                th ID #
                th # Workouts
                th Delete
            each wrestler in wrestlers
                tr
                    td.view: a(href='./wrestlers/'+wrestler.id) View
                    td.name= wrestler.name
                    td.id= wrestler.id
                    td.workouts= wrestler.workouts.length
                    td.delete: a(href='#') X
        //- form(action='/wr/wrestler/add' method='post')
    div
        form(id='wrestler-form')
            input#name(name='name' type='text' placeholder='Name')
            input#age(name='age' type='text' placeholder='Age')
            button#submit(name='submit') Submit
